"""Inicialização de extensões do Flask.

Espaço reservado para extensões futuras (Flask-WTF, Flask-Migrate, etc.).
Quando adicionar uma extensão, instancie-a aqui sem app context e chame
`extension.init_app(app)` em `backend/__init__.py`.
"""
